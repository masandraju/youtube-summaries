{
  "filename": "health_check_api.py",
  "code": "\"\"\"Health Check API Endpoint (SCRUM-7).\n\nProvides a lightweight, cacheable health check endpoint that monitors\nthe running application's status and its dependencies.\n\"\"\"\n\nfrom __future__ import annotations\n\nimport asyncio\nimport logging\nimport os\nimport time\nfrom dataclasses import dataclass, field\nfrom datetime import datetime, timezone\nfrom enum import Enum\nfrom functools import wraps\nfrom typing import Any, Callable, Dict, List, Optional\n\nfrom fastapi import FastAPI, Response\nfrom fastapi.responses import JSONResponse\nimport httpx\nimport uvicorn\n\n# ---------------------------------------------------------------------------\n# Logging\n# ---------------------------------------------------------------------------\n\nlogging.basicConfig(\n    level=logging.INFO,\n    format=\"%(asctime)s [%(levelname)s] %(name)s: %(message)s\",\n)\nlogger = logging.getLogger(__name__)\n\n# ---------------------------------------------------------------------------\n# Enums & Constants\n# ---------------------------------------------------------------------------\n\n\nclass HealthStatus(str, Enum):\n    \"\"\"Possible health statuses for the application or a dependency.\"\"\"\n\n    HEALTHY = \"healthy\"\n    DEGRADED = \"degraded\"\n    UNHEALTHY = \"unhealthy\"\n\n\nHTTP_OK = 200\nHTTP_SERVICE_UNAVAILABLE = 503\n\n# Cache TTL in seconds — avoids hammering dependencies on every request.\nCACHE_TTL_SECONDS: int = int(os.getenv(\"HEALTH_CACHE_TTL\", \"10\"))\n\n# ---------------------------------------------------------------------------\n# Data-transfer objects\n# ---------------------------------------------------------------------------\n\n\n@dataclass\nclass DependencyResult:\n    \"\"\"Result of a single dependency health probe.\"\"\"\n\n    name: str\n    status: HealthStatus\n    response_time_ms: float\n    detail: Optional[str] = None\n\n    def to_dict(self) -> Dict[str, Any]:\n        return {\n            \"name\": self.name,\n            \"status\": self.status.value,\n            \"response_time_ms\": round(self.response_time_ms, 3),\n            \"detail\": self.detail,\n        }\n\n\n@dataclass\nclass HealthReport:\n    \"\"\"Aggregated health report returned by the endpoint.\"\"\"\n\n    status: HealthStatus\n    response_time_ms: float\n    timestamp: str\n    dependencies: List[DependencyResult] = field(default_factory=list)\n    version: str = os.getenv(\"APP_VERSION\", \"1.0.0\")\n\n    def to_dict(self) -> Dict[str, Any]:\n        return {\n            \"status\": self.status.value,\n            \"version\": self.version,\n            \"response_time_ms\": round(self.response_time_ms, 3),\n            \"timestamp\": self.timestamp,\n            \"dependencies\": [d.to_dict() for d in self.dependencies],\n        }\n\n    @property\n    def http_status_code(self) -> int:\n        return HTTP_OK if self.status == HealthStatus.HEALTHY else HTTP_SERVICE_UNAVAILABLE\n\n\n# ---------------------------------------------------------------------------\n# Dependency probes\n# ---------------------------------------------------------------------------\n\n\nasync def probe_database() -> DependencyResult:\n    \"\"\"Probe the database connection.\n\n    Replace the body with your actual DB driver call, e.g.:\n        await db.execute('SELECT 1')\n    \"\"\"\n    db_url = os.getenv(\"DATABASE_URL\", \"\")\n    start = time.perf_counter()\n    try:\n        if not db_url:\n            # No DB configured — treat as not-applicable (healthy with note).\n            elapsed = (time.perf_counter() - start) * 1_000\n            return DependencyResult(\n                name=\"database\",\n                status=HealthStatus.HEALTHY,\n                response_time_ms=elapsed,\n                detail=\"DATABASE_URL not configured; skipped.\",\n            )\n\n        # --- Stub: simulate a lightweight connectivity check ---\n        # In production replace with: await db_pool.fetchval('SELECT 1')\n        await asyncio.sleep(0.001)  # simulated round-trip\n        elapsed = (time.perf_counter() - start) * 1_000\n        return DependencyResult(\n            name=\"database\",\n            status=HealthStatus.HEALTHY,\n            response_time_ms=elapsed,\n        )\n    except Exception as exc:  # noqa: BLE001\n        elapsed = (time.perf_counter() - start) * 1_000\n        logger.warning(\"Database probe failed: %s\", exc)\n        return DependencyResult(\n            name=\"database\",\n            status=HealthStatus.UNHEALTHY,\n            response_time_ms=elapsed,\n            detail=str(exc),\n        )\n\n\nasync def probe_external_service(name: str, url: str, timeout: float = 3.0) -> DependencyResult:\n    \"\"\"Probe an external HTTP service by performing a lightweight GET request.\n\n    Args:\n        name:    Human-readable service name.\n        url:     URL to probe (should be a health / ping endpoint).\n        timeout: Per-request timeout in seconds.\n    \"\"\"\n    start = time.perf_counter()\n    try:\n        async with httpx.AsyncClient(timeout=timeout) as client:\n            resp = await client.get(url)\n        elapsed = (time.perf_counter() - start) * 1_000\n\n        if resp.is_success:\n            return DependencyResult(\n                name=name,\n                status=HealthStatus.HEALTHY,\n                response_time_ms=elapsed,\n                detail=f\"HTTP {resp.status_code}\",\n            )\n        return DependencyResult(\n            name=name,\n            status=HealthStatus.DEGRADED,\n            response_time_ms=elapsed,\n            detail=f\"HTTP {resp.status_code}\",\n        )\n    except Exception as exc:  # noqa: BLE001\n        elapsed = (time.perf_counter() - start) * 1_000\n        logger.warning(\"External service probe '%s' failed: %s\", name, exc)\n        return DependencyResult(\n            name=name,\n            status=HealthStatus.UNHEALTHY,\n            response_time_ms=elapsed,\n            detail=str(exc),\n        )\n\n\n# ---------------------------------------------------------------------------\n# Cache helper\n# ---------------------------------------------------------------------------\n\n\ndef _ttl_cache(ttl_seconds: int) -> Callable:  # type: ignore[type-arg]\n    \"\"\"Simple async-compatible TTL cache decorator.\"\"\"\n\n    def decorator(func: Callable) -> Callable:  # type: ignore[type-arg]\n        _cache: Dict[str, Any] = {\"value\": None, \"expires_at\": 0.0}\n\n        @wraps(func)\n        async def wrapper(*args: Any, **kwargs: Any) -> Any:\n            now = time.monotonic()\n            if _cache[\"value\"] is not None and now < _cache[\"expires_at\"]:\n                logger.debug(\"Returning cached health report.\")\n                return _cache[\"value\"]\n            result = await func(*args, **kwargs)\n            _cache[\"value\"] = result\n            _cache[\"expires_at\"] = now + ttl_seconds\n            return result\n\n        # Expose a manual cache-busting helper for testing.\n        def invalidate() -> None:\n            _cache[\"value\"] = None\n            _cache[\"expires_at\"] = 0.0\n\n        wrapper.invalidate = invalidate  # type: ignore[attr-defined]\n        return wrapper\n\n    return decorator\n\n\n# ---------------------------------------------------------------------------\n# Health aggregation\n# ---------------------------------------------------------------------------\n\n\n@_ttl_cache(ttl_seconds=CACHE_TTL_SECONDS)\nasync def _collect_health() -> HealthReport:\n    \"\"\"Concurrently probe all dependencies and build an aggregated report.\"\"\"\n    overall_start = time.perf_counter()\n\n    # Build probe tasks.  Add / remove entries here to extend coverage.\n    external_services: List[tuple[str, str]] = [\n        (name, url)\n        for name, url in [\n            (\"external_api\", os.getenv(\"EXTERNAL_API_HEALTH_URL\", \"\")),\n        ]\n        if url  # only probe if the env-var is set\n    ]\n\n    tasks: List[asyncio.Task] = [\n        asyncio.create_task(probe_database()),\n        *[\n            asyncio.create_task(probe_external_service(name, url))\n            for name, url in external_services\n        ],\n    ]\n\n    dependency_results: List[DependencyResult] = await asyncio.gather(*tasks)\n\n    total_elapsed = (time.perf_counter() - overall_start) * 1_000\n\n    # Determine overall status.\n    statuses = {r.status for r in dependency_results}\n    if HealthStatus.UNHEALTHY in statuses:\n        overall_status = HealthStatus.UNHEALTHY\n    elif HealthStatus.DEGRADED in statuses:\n        overall_status = HealthStatus.DEGRADED\n    else:\n        overall_status = HealthStatus.HEALTHY\n\n    return HealthReport(\n        status=overall_status,\n        response_time_ms=total_elapsed,\n        timestamp=datetime.now(timezone.utc).isoformat(),\n        dependencies=dependency_results,\n    )\n\n\n# ---------------------------------------------------------------------------\n# FastAPI application\n# ---------------------------------------------------------------------------\n\napp = FastAPI(\n    title=\"Application Health Check API\",\n    description=\"Lightweight health check endpoint (SCRUM-7).\",\n    version=os.getenv(\"APP_VERSION\", \"1.0.0\"),\n)\n\n\n@app.get(\n    \"/health\",\n    summary=\"Application health check\",\n    response_description=\"Aggregated health status of the application and its dependencies.\",\n    tags=[\"health\"],\n)\nasync def health_check(response: Response) -> JSONResponse:\n    \"\"\"Return the aggregated health status of the application.\n\n    **HTTP 200** — application is healthy.\n    **HTTP 503** — application or one or more dependencies are unhealthy.\n\n    The response is cached for ``HEALTH_CACHE_TTL`` seconds (default 10 s)\n    to keep the probe lightweight under high traffic.\n    \"\"\"\n    report = await _collect_health()\n\n    # Signal downstream proxies / CDNs that the response is cacheable.\n    cache_control = f\"public, max-age={CACHE_TTL_SECONDS}, s-maxage={CACHE_TTL_SECONDS}\"\n    response.headers[\"Cache-Control\"] = cache_control\n\n    return JSONResponse(\n        content=report.to_dict(),\n        status_code=report.http_status_code,\n        headers={\"Cache-Control\": cache_control},\n    )\n\n\n@app.get(\n    \"/health/live\",\n    summary=\"Liveness probe\",\